'use client';
import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from 'recharts';
const colors = ['#173d35','#ad8a50','#889e93','#b5bcb3'];
type ChartPoint = { name: string; value: number };
export function SpendCharts({ active = 'Needs', top = [], detail = [], total = 0 }: { active?: string; top?: ChartPoint[]; detail?: ChartPoint[]; total?: number }) { return <div className="space-y-10"><div><div className="mb-3 flex justify-between"><span className="label">Total spend</span><span className="serif text-2xl">${total.toLocaleString()}</span></div><div className="h-56"><ResponsiveContainer><PieChart><Pie data={top} dataKey="value" nameKey="name" innerRadius={58} outerRadius={88} paddingAngle={3}>{top.map((x,i)=><Cell key={x.name} fill={colors[i%colors.length]} />)}</Pie><Tooltip formatter={(v) => `$${Number(v).toLocaleString()}`} /></PieChart></ResponsiveContainer></div><div className="flex justify-center gap-4 text-xs">{top.map((x,i)=><span key={x.name}><i className="mr-1 inline-block h-2 w-2 rounded-full" style={{background:colors[i%colors.length]}} />{x.name}</span>)}</div></div><div><span className="label">{active} breakdown</span><div className="h-56"><ResponsiveContainer><PieChart><Pie data={detail} dataKey="value" nameKey="name" innerRadius={58} outerRadius={88} paddingAngle={3}>{detail.map((x,i)=><Cell key={x.name} fill={colors[i%colors.length]} />)}</Pie><Tooltip formatter={(v) => `$${Number(v).toLocaleString()}`} /></PieChart></ResponsiveContainer></div><div className="grid grid-cols-2 gap-2 text-xs">{detail.map((x,i)=><span key={x.name}><i className="mr-1 inline-block h-2 w-2 rounded-full" style={{background:colors[i%colors.length]}} />{x.name}</span>)}</div></div></div>; }
